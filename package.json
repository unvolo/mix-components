{
  "name": "mix-components",
  "version": "0.1.0-alpha",
  "description": "Mixins and Extendings.",
  "author": "huge-pancake",
  "license": "MIT",
  "main": "",
  "scripts": {
    "build": "tsc --pretty",
    "build:watch": "tsc --pretty --watch",
    "clean": "rm -f src/**/*.js src/**/*.js.map src/**/*.d.ts src/**/**/*.js src/**/**/*.js.map src/**/**/*.d.ts",
    "dev": "web-dev-server",
    "format": "prettier . --write"
  },
  "keywords": [],
  "devDependencies": {
    "@web/dev-server": "^0.1.36",
    "@web/dev-server-esbuild": "^0.3.4",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "tslib": "^2.5.0"
  }
}
